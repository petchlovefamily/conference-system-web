<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Check-in Scanner | Conference</title>
    <link rel="shortcut icon" href="/images/favicon.ico">
    <link href="/css/app.min.css" rel="stylesheet" type="text/css">
    <link href="/css/icons.min.css" rel="stylesheet" type="text/css">
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        /* ========== SCREENS ========== */
        .screen {
            display: none;
            min-height: 100vh;
            flex-direction: column;
            color: white;
        }

        .screen.active {
            display: flex;
        }

        /* ========== EVENT SELECTION ========== */
        .selection-screen {
            padding: 1.5rem;
        }

        .selection-header {
            text-align: center;
            padding: 1.5rem 0;
        }

        .selection-header h1 {
            font-size: 1.4rem;
            margin: 0 0 0.5rem 0;
        }

        .selection-header p {
            opacity: 0.7;
            margin: 0;
            font-size: 0.9rem;
        }

        .selection-list {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .selection-card {
            background: rgba(255, 255, 255, 0.1);
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 16px;
            padding: 1.25rem;
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .selection-card:hover,
        .selection-card:active {
            background: rgba(255, 255, 255, 0.15);
            border-color: #3e60d5;
            transform: scale(1.02);
        }

        .selection-card-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            margin-bottom: 0.75rem;
        }

        .selection-card h3 {
            font-size: 1.1rem;
            font-weight: 600;
            margin: 0;
        }

        .selection-card .badge {
            font-size: 0.7rem;
            padding: 0.2rem 0.6rem;
            border-radius: 20px;
        }

        .badge-primary {
            background: rgba(62, 96, 213, 0.5);
        }

        .badge-success {
            background: rgba(10, 207, 151, 0.5);
        }

        .badge-warning {
            background: rgba(255, 188, 0, 0.5);
            color: #1a1a2e;
        }

        .badge-info {
            background: rgba(80, 195, 230, 0.5);
        }

        .selection-card .info {
            display: flex;
            gap: 1rem;
            font-size: 0.8rem;
            opacity: 0.8;
        }

        .selection-card .info span {
            display: flex;
            align-items: center;
            gap: 0.25rem;
        }

        .selection-card .stats {
            display: flex;
            gap: 0.75rem;
            margin-top: 0.75rem;
            padding-top: 0.75rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .selection-card .stat {
            flex: 1;
            text-align: center;
        }

        .selection-card .stat-value {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .selection-card .stat-label {
            font-size: 0.65rem;
            opacity: 0.7;
            text-transform: uppercase;
        }

        .stat-checked {
            color: #0acf97;
        }

        .stat-pending {
            color: #ffbc00;
        }

        /* Station Cards */
        .station-card {
            position: relative;
            overflow: hidden;
        }

        .station-card .station-icon {
            font-size: 3rem;
            opacity: 0.15;
            position: absolute;
            right: 1rem;
            top: 50%;
            transform: translateY(-50%);
        }

        .station-card.main {
            border-left: 4px solid #3e60d5;
        }

        .station-card.workshop {
            border-left: 4px solid #0acf97;
        }

        .station-card.dinner {
            border-left: 4px solid #fa5c7c;
        }

        .btn-back-link {
            display: block;
            text-align: center;
            padding: 1rem;
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            margin-top: 0.5rem;
        }

        /* Current Selection Breadcrumb */
        .breadcrumb-bar {
            background: rgba(0, 0, 0, 0.3);
            padding: 0.5rem 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.8rem;
        }

        .breadcrumb-bar .divider {
            opacity: 0.5;
        }

        .breadcrumb-bar .current {
            font-weight: 600;
        }

        /* ========== SCANNER SCREEN ========== */
        .app-header {
            background: rgba(0, 0, 0, 0.3);
            padding: 0.75rem 1rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .app-title {
            font-size: 1rem;
            font-weight: 600;
            margin: 0;
        }

        .event-badge {
            font-size: 0.7rem;
            background: rgba(255, 255, 255, 0.2);
            padding: 0.2rem 0.6rem;
            border-radius: 20px;
        }

        .station-banner {
            padding: 0.75rem 1rem;
            text-align: center;
        }

        .station-banner.main-station {
            background: linear-gradient(135deg, #3e60d5 0%, #6366f1 100%);
        }

        .station-banner.workshop-station {
            background: linear-gradient(135deg, #0acf97 0%, #059669 100%);
        }

        .station-banner.dinner-station {
            background: linear-gradient(135deg, #fa5c7c 0%, #dc3545 100%);
        }

        .station-name {
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0;
        }

        .station-desc {
            font-size: 0.75rem;
            opacity: 0.9;
            margin: 0.25rem 0 0 0;
        }

        .btn-change {
            background: rgba(255, 255, 255, 0.2);
            border: none;
            color: white;
            font-size: 0.7rem;
            padding: 0.2rem 0.6rem;
            border-radius: 20px;
            margin-left: 0.5rem;
            cursor: pointer;
        }

        .stats-bar {
            display: flex;
            background: rgba(0, 0, 0, 0.2);
            padding: 0.5rem;
            gap: 0.4rem;
        }

        .stat-item {
            flex: 1;
            text-align: center;
            padding: 0.4rem;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 8px;
        }

        .stat-item .stat-value {
            font-size: 1.25rem;
            font-weight: 700;
        }

        .stat-item .stat-label {
            font-size: 0.6rem;
            opacity: 0.8;
            text-transform: uppercase;
        }

        .stat-item.stat-success .stat-value {
            color: #0acf97;
        }

        .stat-item.stat-pending .stat-value {
            color: #ffbc00;
        }

        .scanner-area {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 1.5rem;
        }

        .scan-frame {
            width: 180px;
            height: 180px;
            border: 3px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1.25rem;
            background: rgba(0, 0, 0, 0.3);
            position: relative;
        }

        .scan-frame::before {
            content: '';
            position: absolute;
            inset: -3px;
            border: 3px solid transparent;
            border-radius: 20px;
            background: linear-gradient(45deg, #0acf97, #3e60d5, #fa5c7c, #ffbc00) border-box;
            mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask: linear-gradient(#fff 0 0) padding-box, linear-gradient(#fff 0 0);
            -webkit-mask-composite: xor;
            mask-composite: exclude;
            animation: borderRotate 3s linear infinite;
        }

        @keyframes borderRotate {
            to {
                filter: hue-rotate(360deg);
            }
        }

        .scan-icon {
            font-size: 48px;
            opacity: 0.5;
        }

        .scan-hint {
            font-size: 0.8rem;
            opacity: 0.7;
            margin-bottom: 1rem;
        }

        .action-buttons {
            display: flex;
            flex-direction: column;
            gap: 0.6rem;
            width: 100%;
            max-width: 260px;
        }

        .btn-scan {
            padding: 0.875rem 1.25rem;
            font-size: 0.95rem;
            font-weight: 600;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .btn-scan-success {
            background: linear-gradient(135deg, #0acf97 0%, #059669 100%);
            color: white;
        }

        .btn-scan-warning {
            background: linear-gradient(135deg, #ffbc00 0%, #f59e0b 100%);
            color: #1a1a2e;
        }

        .btn-scan-error {
            background: linear-gradient(135deg, #fa5c7c 0%, #dc3545 100%);
            color: white;
        }

        .manual-section {
            padding: 0.875rem;
            background: rgba(0, 0, 0, 0.3);
        }

        .manual-input-group {
            display: flex;
            gap: 0.5rem;
        }

        .manual-input {
            flex: 1;
            padding: 0.75rem;
            font-size: 0.95rem;
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.1);
            color: white;
            text-align: center;
            font-family: monospace;
            letter-spacing: 2px;
        }

        .manual-input::placeholder {
            color: rgba(255, 255, 255, 0.4);
        }

        .btn-check {
            padding: 0.75rem 1rem;
            background: #3e60d5;
            color: white;
            border: none;
            border-radius: 10px;
            font-weight: 600;
        }

        /* ========== RESULT MODAL ========== */
        .result-modal-backdrop {
            position: fixed;
            inset: 0;
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            padding: 1.5rem;
        }

        .result-modal-backdrop.show {
            display: flex;
            animation: fadeIn 0.2s ease;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .result-modal {
            background: white;
            border-radius: 24px;
            padding: 2rem 1.5rem 1.5rem;
            width: 100%;
            max-width: 320px;
            box-shadow: 0 25px 60px rgba(0, 0, 0, 0.4);
            text-align: center;
            animation: modalSlideUp 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        @keyframes modalSlideUp {
            from {
                transform: translateY(30px) scale(0.95);
                opacity: 0;
            }

            to {
                transform: translateY(0) scale(1);
                opacity: 1;
            }
        }

        /* Status Header Bar */
        .result-modal .status-bar {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 6px;
        }

        .result-modal.success .status-bar {
            background: linear-gradient(90deg, #0acf97 0%, #059669 100%);
        }

        .result-modal.warning .status-bar {
            background: linear-gradient(90deg, #ffbc00 0%, #f59e0b 100%);
        }

        .result-modal.error .status-bar {
            background: linear-gradient(90deg, #fa5c7c 0%, #dc3545 100%);
        }

        .result-modal.no-access .status-bar {
            background: linear-gradient(90deg, #6c757d 0%, #495057 100%);
        }

        /* Icon Circle */
        .result-icon-wrapper {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            animation: iconPop 0.4s ease 0.1s both;
        }

        @keyframes iconPop {
            0% {
                transform: scale(0);
            }

            60% {
                transform: scale(1.15);
            }

            100% {
                transform: scale(1);
            }
        }

        .result-modal.success .result-icon-wrapper {
            background: linear-gradient(135deg, #0acf97 0%, #059669 100%);
        }

        .result-modal.warning .result-icon-wrapper {
            background: linear-gradient(135deg, #ffbc00 0%, #f59e0b 100%);
        }

        .result-modal.error .result-icon-wrapper {
            background: linear-gradient(135deg, #fa5c7c 0%, #dc3545 100%);
        }

        .result-modal.no-access .result-icon-wrapper {
            background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
        }

        .result-icon {
            font-size: 48px;
            color: white;
        }

        /* Success checkmark animation */
        .result-modal.success .result-icon {
            animation: checkBounce 0.5s ease 0.2s both;
        }

        @keyframes checkBounce {
            0% {
                transform: scale(0) rotate(-45deg);
            }

            50% {
                transform: scale(1.2) rotate(0deg);
            }

            100% {
                transform: scale(1) rotate(0deg);
            }
        }

        .result-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.75rem;
            color: #1a1a2e;
        }

        .result-modal.success .result-title {
            color: #059669;
        }

        .result-modal.warning .result-title {
            color: #d97706;
        }

        .result-modal.error .result-title {
            color: #dc3545;
        }

        .result-modal.no-access .result-title {
            color: #6c757d;
        }

        .result-avatar {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            border: 4px solid #e5e7eb;
            margin-bottom: 0.75rem;
            object-fit: cover;
        }

        .result-name {
            font-size: 1.2rem;
            font-weight: 600;
            color: #1a1a2e;
            margin-bottom: 0.25rem;
        }

        .result-role {
            display: inline-block;
            background: linear-gradient(135deg, #3e60d5 0%, #6366f1 100%);
            color: white;
            padding: 0.35rem 0.75rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 500;
            margin-bottom: 0.5rem;
        }

        .result-ticket {
            display: inline-block;
            background: #f3f4f6;
            color: #4b5563;
            padding: 0.4rem 0.8rem;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .result-code {
            font-family: 'SF Mono', Monaco, monospace;
            font-size: 0.9rem;
            margin-top: 0.75rem;
            color: #6b7280;
            background: #f9fafb;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            display: inline-block;
        }

        .result-message {
            font-size: 0.95rem;
            color: #6b7280;
            margin-top: 0.75rem;
            line-height: 1.5;
        }

        .result-message strong {
            color: #374151;
        }

        .btn-next {
            margin-top: 1.5rem;
            padding: 0.875rem 2.5rem;
            font-size: 1rem;
            font-weight: 600;
            color: white;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.2s ease;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .btn-next:active {
            transform: scale(0.95);
        }

        .result-modal.success .btn-next {
            background: linear-gradient(135deg, #0acf97 0%, #059669 100%);
        }

        .result-modal.warning .btn-next {
            background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
        }

        .result-modal.error .btn-next {
            background: linear-gradient(135deg, #fa5c7c 0%, #dc3545 100%);
        }

        .result-modal.no-access .btn-next {
            background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
        }

        /* Timestamp */
        .result-time {
            font-size: 0.75rem;
            color: #9ca3af;
            margin-top: 1rem;
        }

        .btn-back {
            background: none;
            border: none;
            color: white;
            font-size: 1.4rem;
            cursor: pointer;
            padding: 0.5rem;
        }
    </style>
</head>

<body>

    <!-- ========== SCREEN 1: EVENT SELECTION ========== -->
    <div class="screen selection-screen active" id="eventScreen">
        <div class="selection-header">
            <h1><i class="ti ti-calendar-event me-2"></i>เลือก Event</h1>
            <p>เลือกงานที่ต้องการ Check-in</p>
        </div>

        <div class="selection-list" id="eventList">
            <div class="selection-card" onclick="selectEvent(1, 'ACCP Annual Conference 2026', 'ACCP2026')">
                <div class="selection-card-header">
                    <h3>ACCP Annual Conference 2026</h3>
                    <span class="badge badge-primary">ACCP2026</span>
                </div>
                <div class="info">
                    <span><i class="ti ti-calendar"></i> 15-17 Mar 2026</span>
                    <span><i class="ti ti-map-pin"></i> QSNCC</span>
                </div>
                <div class="stats">
                    <div class="stat">
                        <div class="stat-value stat-checked">18</div>
                        <div class="stat-label">Checked In</div>
                    </div>
                    <div class="stat">
                        <div class="stat-value stat-pending">307</div>
                        <div class="stat-label">Pending</div>
                    </div>
                    <div class="stat">
                        <div class="stat-value">325</div>
                        <div class="stat-label">Total</div>
                    </div>
                </div>
            </div>

            <div class="selection-card" onclick="selectEvent(2, 'Medical Innovation Summit', 'MIS2026')">
                <div class="selection-card-header">
                    <h3>Medical Innovation Summit</h3>
                    <span class="badge badge-info">MIS2026</span>
                </div>
                <div class="info">
                    <span><i class="ti ti-calendar"></i> 20-22 Apr 2026</span>
                    <span><i class="ti ti-map-pin"></i> QSNCC</span>
                </div>
                <div class="stats">
                    <div class="stat">
                        <div class="stat-value stat-checked">5</div>
                        <div class="stat-label">Checked In</div>
                    </div>
                    <div class="stat">
                        <div class="stat-value stat-pending">45</div>
                        <div class="stat-label">Pending</div>
                    </div>
                    <div class="stat">
                        <div class="stat-value">50</div>
                        <div class="stat-label">Total</div>
                    </div>
                </div>
            </div>
        </div>

        <a href="/checkin-scanner" class="btn-back-link"><i class="ti ti-arrow-left me-1"></i> กลับหน้า Admin</a>
    </div>

    <!-- ========== SCREEN 2: STATION SELECTION ========== -->
    <div class="screen selection-screen" id="stationScreen">
        <div class="breadcrumb-bar">
            <span id="breadcrumbEvent">ACCP 2026</span>
            <span class="divider">›</span>
            <span class="current">เลือก Station</span>
        </div>

        <div class="selection-header">
            <h1><i class="ti ti-building me-2"></i>เลือก Station</h1>
            <p>เลือกจุด Check-in ที่คุณรับผิดชอบ</p>
        </div>

        <div class="selection-list" id="stationList">
            <!-- Dynamic content -->
        </div>

        <a href="javascript:void(0)" onclick="backToEvents()" class="btn-back-link"><i
                class="ti ti-arrow-left me-1"></i> เปลี่ยน Event</a>
    </div>

    <!-- ========== SCREEN 3: SCANNER ========== -->
    <div class="screen" id="scannerScreen">
        <div class="app-header">
            <button class="btn-back" onclick="backToStations()"><i class="ti ti-arrow-left"></i></button>
            <h1 class="app-title"><i class="ti ti-scan me-2"></i>Check-in</h1>
            <span class="event-badge" id="scannerEventBadge">ACCP2026</span>
        </div>

        <div class="station-banner main-station" id="stationBanner">
            <p class="station-name" id="stationName">Main Conference</p>
            <p class="station-desc" id="stationDesc">ห้องประชุมหลัก</p>
        </div>

        <div class="stats-bar">
            <div class="stat-item stat-success">
                <div class="stat-value" id="statCheckedIn">0</div>
                <div class="stat-label">Checked In</div>
            </div>
            <div class="stat-item stat-pending">
                <div class="stat-value" id="statPending">325</div>
                <div class="stat-label">Pending</div>
            </div>
            <div class="stat-item">
                <div class="stat-value" id="statPercent">0%</div>
                <div class="stat-label">Complete</div>
            </div>
        </div>

        <div class="scanner-area">
            <div class="scan-frame"><i class="ti ti-scan scan-icon"></i></div>
            <p class="scan-hint">กดปุ่มเพื่อจำลองการ Scan</p>
            <div class="action-buttons">
                <button class="btn-scan btn-scan-success" onclick="simulateScan('success')"><i class="ti ti-check"></i>
                    Scan ผ่าน</button>
                <button class="btn-scan btn-scan-warning" onclick="simulateScan('duplicate')"><i
                        class="ti ti-alert-triangle"></i> เช็คอินแล้ว</button>
                <button class="btn-scan btn-scan-error" onclick="simulateScan('no-access')"><i class="ti ti-lock"></i>
                    ไม่มีสิทธิ์เข้า</button>
            </div>
        </div>

        <div class="manual-section">
            <div class="manual-input-group">
                <input type="text" class="manual-input" id="manualCode" placeholder="REG-XXX-000">
                <button class="btn-check" onclick="manualCheckIn()"><i class="ti ti-check"></i></button>
            </div>
        </div>
    </div>

    <!-- Result Modal -->
    <div class="result-modal-backdrop" id="resultModalBackdrop">
        <div class="result-modal" id="resultModal">
            <div class="status-bar"></div>
            <div id="resultContent"></div>
        </div>
    </div>

    <script src="/js/vendor.min.js"></script>
    <script>
        // State
        let currentEvent = null;
        let currentStation = null;
        let checkedInCount = 0;
        let eligibleCount = 0;

        // Events & Stations Data
        const eventsData = {
            1: {
                name: 'ACCP Annual Conference 2026',
                code: 'ACCP2026',
                stations: [
                    { id: 'conference', name: 'Conference', desc: 'ห้องประชุมหลัก - ผู้ลงทะเบียนทุกคน', icon: 'ti-building', class: 'main', eligible: 325, checkedIn: 18 },
                    { id: 'preconference', name: 'Pre-Conference Workshop', desc: 'Workshop ก่อนงาน - เฉพาะผู้ซื้อ Add-on', icon: 'ti-tool', class: 'workshop', eligible: 85, checkedIn: 5 },
                    { id: 'dinner', name: 'Gala Dinner', desc: 'งานเลี้ยง - เฉพาะผู้ซื้อ Add-on', icon: 'ti-glass-full', class: 'dinner', eligible: 120, checkedIn: 0 }
                ],
                registrations: {
                    'REG-ABC-001': { name: 'Dr. Somchai Jaidee', role: 'Thai Pharmacy', ticket: 'Conference + Pre-Conference Workshop + Gala Dinner', access: ['conference', 'preconference', 'dinner'], checkedIn: {} },
                    'REG-DEF-002': { name: 'Nattaporn Srisuk', role: 'Thai Student', ticket: 'Conference + Pre-Conference Workshop', access: ['conference', 'preconference'], checkedIn: {} },
                    'REG-GHI-003': { name: 'Prof. Wichai Tanaka', role: 'Thai Academician', ticket: 'Conference + Gala Dinner', access: ['conference', 'dinner'], checkedIn: {} },
                    'REG-JKL-004': { name: 'John Smith', role: 'International Student', ticket: 'Conference Only', access: ['conference'], checkedIn: {} },
                    'REG-MNO-005': { name: 'Prof. Sarah Johnson', role: 'International Professional & Academician', ticket: 'Conference + Pre-Conference Workshop + Gala Dinner', access: ['conference', 'preconference', 'dinner'], checkedIn: {} }
                }
            },
            2: {
                name: 'Medical Innovation Summit',
                code: 'MIS2026',
                stations: [
                    { id: 'conference', name: 'Main Hall', desc: 'ห้องประชุมหลัก', icon: 'ti-building', class: 'main', eligible: 50, checkedIn: 5 }
                ],
                registrations: {
                    'REG-MIS-001': { name: 'Supaporn Chai', role: 'Thai Pharmacy', ticket: 'Standard', access: ['conference'], checkedIn: {} },
                    'REG-MIS-002': { name: 'Piyapong Suwannee', role: 'Thai Academician', ticket: 'VIP', access: ['conference'], checkedIn: {} }
                }
            }
        };

        let codeIndex = 0;

        function showScreen(screenId) {
            document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
            document.getElementById(screenId).classList.add('active');
        }

        function selectEvent(eventId, eventName, eventCode) {
            currentEvent = eventsData[eventId];
            document.getElementById('breadcrumbEvent').textContent = eventName;

            // Build station list
            const stationList = document.getElementById('stationList');
            stationList.innerHTML = currentEvent.stations.map(station => `
                <div class="selection-card station-card ${station.class}" onclick="selectStation('${station.id}')">
                    <i class="ti ${station.icon} station-icon"></i>
                    <div class="selection-card-header">
                        <h3>${station.name}</h3>
                        <span class="badge badge-${station.class === 'main' ? 'primary' : station.class === 'workshop' ? 'success' : 'warning'}">${station.eligible} คน</span>
                    </div>
                    <div class="info"><span>${station.desc}</span></div>
                    <div class="stats">
                        <div class="stat"><div class="stat-value stat-checked">${station.checkedIn}</div><div class="stat-label">Checked In</div></div>
                        <div class="stat"><div class="stat-value stat-pending">${station.eligible - station.checkedIn}</div><div class="stat-label">Pending</div></div>
                    </div>
                </div>
            `).join('');

            showScreen('stationScreen');
        }

        function selectStation(stationId) {
            currentStation = currentEvent.stations.find(s => s.id === stationId);
            checkedInCount = currentStation.checkedIn;
            eligibleCount = currentStation.eligible;
            codeIndex = 0;

            // Update UI
            document.getElementById('scannerEventBadge').textContent = currentEvent.code;
            document.getElementById('stationName').textContent = currentStation.name;
            document.getElementById('stationDesc').textContent = currentStation.desc;

            const banner = document.getElementById('stationBanner');
            banner.className = 'station-banner ' + currentStation.class + '-station';

            updateStats();
            showScreen('scannerScreen');
        }

        function backToEvents() { showScreen('eventScreen'); }
        function backToStations() { showScreen('stationScreen'); }

        function getCurrentTime() {
            return new Date().toLocaleTimeString('th-TH', { hour: '2-digit', minute: '2-digit', second: '2-digit' });
        }

        function showResultModal(type, html) {
            const backdrop = document.getElementById('resultModalBackdrop');
            const modal = document.getElementById('resultModal');
            const content = document.getElementById('resultContent');

            // Reset classes
            modal.className = 'result-modal ' + type;

            content.innerHTML = html;
            backdrop.classList.add('show');

            if (navigator.vibrate) {
                navigator.vibrate(type === 'success' ? [100] : [100, 50, 100]);
            }
        }

        function simulateScan(type) {
            const codes = Object.keys(currentEvent.registrations);
            let html = '';

            if (type === 'success') {
                const code = codes[codeIndex % codes.length];
                const reg = currentEvent.registrations[code];

                // Check if already checked in at this station
                if (reg.checkedIn[currentStation.id]) {
                    simulateScan('duplicate');
                    return;
                }

                // Check if has access to this station
                if (!reg.access.includes(currentStation.id)) {
                    simulateScan('no-access');
                    return;
                }

                reg.checkedIn[currentStation.id] = true;
                checkedInCount++;
                codeIndex++;
                updateStats();

                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-circle-check result-icon"></i>
                    </div>
                    <div class="result-title">Check-in สำเร็จ!</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <div class="result-ticket">${reg.ticket}</div>
                    <div class="result-code">${code}</div>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
                showResultModal('success', html);
            } else if (type === 'duplicate') {
                const code = codes[(codeIndex > 0 ? codeIndex - 1 : 0) % codes.length];
                const reg = currentEvent.registrations[code];

                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-alert-triangle result-icon"></i>
                    </div>
                    <div class="result-title">เช็คอินแล้ว</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <p class="result-message">
                        ลงทะเบียนที่ <strong>${currentStation.name}</strong> แล้ว
                    </p>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
                showResultModal('warning', html);
            } else if (type === 'no-access') {
                const code = codes[codeIndex % codes.length];
                const reg = currentEvent.registrations[code] || { name: 'Unknown', role: 'Unknown', ticket: 'Unknown' };

                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-lock result-icon"></i>
                    </div>
                    <div class="result-title">ไม่มีสิทธิ์เข้างาน</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <div class="result-ticket">${reg.ticket}</div>
                    <p class="result-message">
                        <i class="ti ti-x"></i> ไม่ได้ซื้อ Add-on สำหรับ <strong>${currentStation.name}</strong>
                    </p>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
                showResultModal('no-access', html);
            }
        }

        function manualCheckIn() {
            const code = document.getElementById('manualCode').value.trim().toUpperCase();
            if (!code) return;

            const reg = currentEvent.registrations[code];
            let html = '';
            let type = '';

            if (!reg) {
                type = 'error';
                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-circle-x result-icon"></i>
                    </div>
                    <div class="result-title">ไม่พบรหัส</div>
                    <div class="result-code">${code}</div>
                    <p class="result-message">กรุณาตรวจสอบรหัสลงทะเบียนและลองใหม่อีกครั้ง</p>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-refresh"></i> ลองใหม่
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
            } else if (!reg.access.includes(currentStation.id)) {
                type = 'no-access';
                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-lock result-icon"></i>
                    </div>
                    <div class="result-title">ไม่มีสิทธิ์เข้างาน</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <p class="result-message">ไม่ได้ซื้อ Add-on สำหรับ <strong>${currentStation.name}</strong></p>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
            } else if (reg.checkedIn[currentStation.id]) {
                type = 'warning';
                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-alert-triangle result-icon"></i>
                    </div>
                    <div class="result-title">เช็คอินแล้ว</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <p class="result-message">ลงทะเบียนที่ <strong>${currentStation.name}</strong> แล้ว</p>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
            } else {
                reg.checkedIn[currentStation.id] = true;
                checkedInCount++;
                updateStats();
                type = 'success';
                html = `
                    <div class="result-icon-wrapper">
                        <i class="ti ti-circle-check result-icon"></i>
                    </div>
                    <div class="result-title">Check-in สำเร็จ!</div>
                    <div class="result-name">${reg.name}</div>
                    <div class="result-role">${reg.role}</div>
                    <div class="result-ticket">${reg.ticket}</div>
                    <button class="btn-next" onclick="closeResult()">
                        <i class="ti ti-scan"></i> Scan ต่อ
                    </button>
                    <div class="result-time"><i class="ti ti-clock"></i> ${getCurrentTime()}</div>
                `;
            }

            showResultModal(type, html);
            document.getElementById('manualCode').value = '';
        }

        function closeResult() {
            document.getElementById('resultModalBackdrop').classList.remove('show');
        }

        function updateStats() {
            document.getElementById('statCheckedIn').textContent = checkedInCount;
            document.getElementById('statPending').textContent = eligibleCount - checkedInCount;
            document.getElementById('statPercent').textContent = ((checkedInCount / eligibleCount) * 100).toFixed(0) + '%';
        }

        document.getElementById('manualCode').addEventListener('keypress', function (e) { if (e.key === 'Enter') manualCheckIn(); });
    </script>
</body>

</html>